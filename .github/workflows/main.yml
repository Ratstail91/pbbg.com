name: main
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Install packages
        run: composer install
      - name: Run PHPlint
        run: composer lint
      - name: Run PHPunit
        run: composer test
